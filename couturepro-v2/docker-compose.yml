version: '3.8'

services:
  backend:
    build: .
    container_name: couturepro-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - .:/app
    env_file:
      - .env
    restart: unless-stopped
